import styled from "styled-components";

const MembersWrapper = styled.div`
    padding: 24vh 12vw 12vh;
    justify-content: center;
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 2vw;
`;

export default MembersWrapper;